# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_DockerPushDevWeb --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: DockerPushDevWeb

on:
  push:
    branches:
      - main
    paths:
      - 'src/StammPhoenix.Build/**'
      - 'src/StammPhoenix.Web/**'
  workflow_dispatch:
    inputs:
      description:
        description: "description"
        required: false

permissions:
  contents: write
  packages: write

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    concurrency:
      group: DockerPushDevWeb
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: DockerPushDevWeb'
        run: ./build.cmd DockerPushDevWeb
        env:
          description: ${{ github.event.inputs.description }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
