---
// src/components/GlassMorphism.astro
const { class: className = '' } = Astro.props
---

<div
	class={`m-[1rem] p-[0.5rem] bg-gray-300 rounded-lg bg-clip-padding backdrop-filter backdrop-blur-lg bg-opacity-30 border ${className}`}
>
	<slot />
</div>
