namespace StammPhoenix.Api.Core;

public static class EnvironmentNames
{
    public const string LOG_PATH = nameof(LOG_PATH);

    public const string CONFIG_PATH = nameof(CONFIG_PATH);

    public const string PUBLIC_SIGNING_KEY = nameof(PUBLIC_SIGNING_KEY);

    public const string PRIVATE_SIGNING_KEY = nameof(PRIVATE_SIGNING_KEY);

    public const string ALLOWED_HOSTS = nameof(ALLOWED_HOSTS);

    public const string DATABASE_NAME = nameof(DATABASE_NAME);

    public const string DATABASE_PORT = nameof(DATABASE_PORT);

    public const string DATABASE_HOST = nameof(DATABASE_HOST);

    public const string DATABASE_USER = nameof(DATABASE_USER);

    public const string DATABASE_PASSWORD = nameof(DATABASE_PASSWORD);
}
